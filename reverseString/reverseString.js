const reverseString = function(str) {
      // var myString = str.split("");
    // var reverseArray = myString.reverse();
    // var joinArray = reverseArray.join("");
    // return joinArray;


          // same as above: 
    return str.split("").reverse().join("");

}

module.exports = reverseString
